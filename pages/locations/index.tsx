import List from "@/components/listFilterPage/index";
import { DataTypes } from "@/types/types";
const Locations: React.FC = () => {
  return <List type={DataTypes.locations} />;
};
export default Locations;
